This folder holds site images for the Nature-Forward Therapy landing page.

Suggested filenames:
- `hero-bg.jpg` – main hero background image
- `nicole-headshot.jpg` – circular headshot in the hero section

After adding files, update the image paths in `index.html` to point to `./images/...`.
